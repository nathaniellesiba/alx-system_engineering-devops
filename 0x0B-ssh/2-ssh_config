# ssh client system-wide config file.
# defaults for users, and the values can be changed
# in per-user configuration files or on the command line.
Host *
        IdentityFile ~/.ssh/school
        PasswordAuthentication no
        HostbasedAuthentication noi
